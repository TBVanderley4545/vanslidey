/**
 * Initialize the carousel.
 *
 * @param carouselContainer The container to use for the carousel.
 */
export declare const initCarousel: (carouselContainer: HTMLElement) => void;
/**
 * Remove the carousel.
 *
 * @param carouselContainer The container to use for the carousel.
 */
export declare const deinitCarousel: (carouselContainer: HTMLElement) => void;
